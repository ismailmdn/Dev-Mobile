<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projetguermah">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetGuermah"
        tools:targetApi="31">

        <!-- AuthActivity is the entry point -->
        <activity android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity should also be declared -->
        <activity android:name=".MainActivity"
            android:exported="false" />

        <!-- AddTransactionActivity -->
        <activity android:name=".AddTransactionActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjetGuermah.NoActionBar" />
            
        <!-- TransactionDetailsActivity -->
        <activity android:name=".TransactionDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.ProjetGuermah.NoActionBar" />

        <activity android:name=".SavingsGoalsActivity" />
        <activity android:name=".SavingChallengesActivity" />
        <activity android:name=".AddGoalActivity" />
    </application>
</manifest>
